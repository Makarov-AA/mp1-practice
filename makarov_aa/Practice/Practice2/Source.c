#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int b[10];
void main() {
	setlocale(LC_ALL, "Russian");
	int a[D] = { 0 }; // ������ ������� ���� � �����
	int b[D] = { 0 }; // ������ ��� �������� ��������������� ��������� ������������� �����
	int n; //����� �����
	int g; //������������� �����
	int i;//�������
	int check1, check2;//���������� ��� �������� ��������� ����� �� ����� � ���������������
	int cows, beefs;//���� � ������
	long long int k, p, p1;//���������� � �������� �����, �������������� ����� ��� ���������
	printf("������� ����� ������������� ����� \n");
	do {
		scanf("%d", &n);
	} while ((n <= 0) || (n > 10)); // ���� ����� �����
	i = 1;
	k = 0;
	srand((unsigned int)time(0));
	g = rand() * 8 / RAND_MAX + 1;//��������� ��������� ��� ������ ����� �� ������ 0
	k += g;
	a[g] += i;
	while (i < n) { //��������� ��������� ���� � ���������� �� ������� � ������
		g = rand() * 9 / RAND_MAX;
		if (a[g] == 0) {
			k *= 10;
			k += g;
			a[g] += i + 1;
			i++;
		}
	}
	printf("%lld ��������� ����� (����� � ������ �� ������ �����������)\n", k);
	do {
		do { //�������� ��������� �����
			scanf("%lld", &p);
			p1 = p;
			for (i = 0; i < D; i++)
				b[i] = 0;
			check1 = 0;
			while (p1 != 0) { //�������� �� �����
				b[p1 % 10]++;
				p1 /= 10;
				check1++;
			}
			check2 = 0;
			for (i = 0; i < D; i++) //�������� �� ���������������
				if (b[i] > 1)
					check2++;
		} while ((check1 != n) || (check2 != 0));
		i = n;
		cows = 0;
		beefs = 0;
		while (p != 0) {//�������� �� ���������� ����� � �����
			if (a[p % 10] > 0)
				cows++;
			if (a[p % 10] == i)
				beefs++;
			i--;
			p /= 10;
		}
		printf("%d �����, %d �����\n", cows, beefs);//����� ������
	} while ((cows != n) || (beefs != n));// ���������, ���� ����� �� �������� ���
	printf("�����������, �� ��������!");
}