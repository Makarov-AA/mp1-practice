#include <stdio.h>
#include <stdlib.h>
#include <time.h>
void main() {
	int mode;//режима работы
	printf("Выберите режим работы:\n1 - отгадывает пользователь\n2 - отгадывает компьютер");
	do {
		scanf("%d", &mode);
		while ((mode != 1) || (mode != 2))
	if (mode == )
}